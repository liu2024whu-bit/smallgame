function openIntro() { state.introSeen = true; saveState(); openModal({ eyebrow: 'PROLOGUE / 23:59', title: STORY.shortTitle, wide: true, content: `<div class="prologue-layout"><div class="prologue-tree" aria-hidden="true"><span>19</span><i></i><b>+1</b></div><div><p class="prologue-lead">${escapeHTML(STORY.intro)}</p><blockquote>“有些年份已经长成树干，有些年份仍是花苞。初版只负责把门打开，真正的故事可以以后继续补上。”</blockquote><div class="prologue-facts"><span><strong>1029</strong>重要日期</span><span><strong>LXY</strong>名字缩写</span><span><strong>20</strong>新的年轮</span></div><div class="puzzle-actions"><button class="button button--primary" type="button" id="beginGame">走到树下</button></div></div></div>`, onOpen: () => document.querySelector('#beginGame').addEventListener('click', () => { closeModal(); highlightObject('calendar'); }) }); }
function openCalendar() { if (markDiscovery('calendar')) addJournal('calendar', '枝头的日历停在 10 月 29 日，纸角写着“月在前，日在后”。'); setSceneMessage('10 月 29 日。月份与日期都写成两位数，正好能组成四位密码。'); simpleObjectModal({ eyebrow: 'CLUE / DATE', title: '只圈出一天的日历', visual: '<div class="calendar-detail"><strong>OCT</strong><span>29</span><small>月在前，日在后</small></div>', copy: '整本日历只有 <strong>10 月 29 日</strong> 被金色笔反复圈过。旁边的小箭头从“月”指向“日”，像是在告诉你书写顺序。', clues: ['树根处有一把四位数锁。', '10 月需要写成两位数。'] }); }
function openRoot() { if (state.rootUnlocked) { simpleObjectModal({ eyebrow: 'MEMORY 01 / RESTORED', title: '已经打开的树根匣', visual: '<div class="root-box-detail"><span>OPEN</span><i>▤</i></div>', copy: state.monogramSolved ? '七张成长卡已经挂回枝头。木匣里只剩一张便签：<strong>“别急着把未来写完。”</strong>' : '木匣中放着七张成长卡，从初三一直写到大三。卡片顺序被打乱，每张角落还有一个单独的字母。', clues: state.monogramSolved ? [] : ['把它们带到枝头的时间线上。'], action: state.monogramSolved ? '' : '<button class="button button--primary inline-action" type="button" id="goTimeline">查看时间线</button>' }); document.querySelector('#goTimeline')?.addEventListener('click', openTimeline); return; } openRootKeypad(); }
function openRootKeypad() { openModal({ eyebrow: 'PUZZLE 01 / ROOT', title: '树根上的四位数锁', content: `<div class="puzzle-intro"><span class="puzzle-intro__mark">1</span><span>锁旁刻着：<strong>“先写月份，再写那一天。”</strong></span></div><form id="rootForm"><div class="keypad-shell"><div class="keypad-display"><input id="rootCode" type="text" inputmode="numeric" maxlength="4" autocomplete="off" placeholder="••••" aria-label="四位数字密码" /></div><div class="keypad-grid" aria-label="数字键盘">${[1,2,3,4,5,6,7,8,9].map((digit) => `<button class="keypad-key" type="button" data-key="${digit}">${digit}</button>`).join('')}<button class="keypad-key" type="button" data-key="clear">C</button><button class="keypad-key" type="button" data-key="0">0</button><button class="keypad-key" type="button" data-key="back" aria-label="退格">←</button></div></div><p class="inline-feedback" id="rootFeedback" aria-live="polite"></p><div class="puzzle-actions"><button class="button button--ghost" type="button" data-close-modal>先去调查</button><button class="button button--primary" type="submit">打开木匣</button></div></form>`, onOpen: () => { const form = document.querySelector('#rootForm'); const input = document.querySelector('#rootCode'); const feedback = document.querySelector('#rootFeedback'); input.addEventListener('input', () => { input.value = normalizeDigits(input.value); }); form.querySelectorAll('[data-key]').forEach((button) => button.addEventListener('click', () => { playClick(); const key = button.dataset.key; if (key === 'clear') input.value = ''; else if (key === 'back') input.value = input.value.slice(0, -1); else input.value = normalizeDigits(`${input.value}${key}`); input.focus(); })); form.addEventListener('submit', (event) => { event.preventDefault(); if (!isValidRootCode(input.value)) { feedback.textContent = '锁没有打开。日期顺序可能反了，或月份没有写成两位数。'; feedback.className = 'inline-feedback is-error'; playError(); return; } state.rootUnlocked = true; addJournal('root', '输入 1029 后，树根匣打开，里面是七张被打乱的成长卡。'); saveState(); render(); playSuccess(); feedback.textContent = '锁芯弹开。七张卡片像一小叠叶子。'; feedback.className = 'inline-feedback is-success'; window.setTimeout(() => { closeModal(); showToast('第一段记忆恢复：获得七张成长卡。', 'success'); highlightObject('timeline'); }, 620); }); } }); }
function openCat() { if (markDiscovery('cat')) addJournal('cat', '猫的项圈上刻着三个数字：2、5、7。它总抬头看向枝头的成长卡。'); simpleObjectModal({ eyebrow: 'COMPANION / CAT', title: '戴着数字项圈的猫', visual: '<div class="pet-detail pet-detail--cat"><span>◖•ᴥ•◗</span><strong>2 · 5 · 7</strong></div>', copy: '猫不肯让人碰项圈，却故意把金属牌转到正面。上面只有 <strong>2 · 5 · 7</strong>。它看一眼你，又看一眼枝头的七张卡。', clues: ['数字像是在指定卡片的位置。', '先把时间排正确，再取第 2、5、7 张。'] }); }
function openDog() { if (markDiscovery('dog')) addJournal('dog', '狗的吊牌背面写着“从最早的那一年，读向现在”。'); simpleObjectModal({ eyebrow: 'COMPANION / DOG', title: '守在树根旁的狗', visual: '<div class="pet-detail pet-detail--dog"><span>ᵔᴥᵔ</span><strong>最早 → 现在</strong></div>', copy: '狗把吊牌推到你手边。背面刻着：<strong>“从最早的那一年，读向现在。”</strong>它守着树根，似乎比谁都清楚故事从哪里开始。', clues: ['成长卡要按学段的先后顺序排列。', '读取方向是从左到右。'] }); }

function timelineCardMarkup(cardId, index) { const card = CARD_BY_ID.get(cardId); return `<article class="timeline-card" data-card-id="${card.id}"><div class="timeline-card__top"><span>${index + 1}</span><b>${escapeHTML(card.glyph)}</b></div><strong>${escapeHTML(card.stage)}</strong><small>${escapeHTML(card.flower)}</small><p>${escapeHTML(card.note)}</p><em>${escapeHTML(card.letter)}</em><div class="timeline-card__moves"><button type="button" data-move="-1" data-index="${index}" aria-label="把 ${escapeHTML(card.stage)} 向左移动" ${index === 0 ? 'disabled' : ''}>←</button><button type="button" data-move="1" data-index="${index}" aria-label="把 ${escapeHTML(card.stage)} 向右移动" ${index === state.timelineOrder.length - 1 ? 'disabled' : ''}>→</button></div></article>`; }
function openTimeline() { if (!state.rootUnlocked) { simpleObjectModal({ eyebrow: 'LOCKED / TIMELINE', title: '只有夹子的空绳', visual: '<div class="locked-detail">▱ ▱ ▱ ▱ ▱ ▱ ▱</div>', copy: '枝头挂着七枚空夹子。树根匣的尺寸，似乎正好能装下对应的卡片。', clues: ['先打开树根上的四位数锁。'] }); return; } if (state.monogramSolved) { openSolvedTimeline(); return; } openTimelinePuzzle(); }
function openTimelinePuzzle() { const ordered = isTimelineSolved(state.timelineOrder); if (ordered && !state.timelineOrdered) { state.timelineOrdered = true; addJournal('timeline-order', '七张卡按初三、高一、高二、高三、大一、大二、大三排回了正确顺序。'); saveState(); } openModal({ eyebrow: 'PUZZLE 02 / YEARS', title: state.timelineOrdered ? '读取第 2、5、7 张卡' : '把时间排回正确顺序', wide: true, content: `<div class="puzzle-intro"><span class="puzzle-intro__mark">2</span><span>${state.timelineOrdered ? '猫给出位置，狗给出方向。按顺序读取卡片角落的小字母。' : '使用左右箭头，把七个学段从最早排到现在。卡片角落的字母暂时不要急着读。'}</span></div><div class="timeline-strip" id="timelineStrip">${state.timelineOrder.map(timelineCardMarkup).join('')}</div><p class="inline-feedback ${state.timelineOrdered ? 'is-success' : ''}" id="timelineFeedback" aria-live="polite">${state.timelineOrdered ? '时间顺序正确。现在读取第 2、5、7 张。' : '当前顺序尚未确认。'}</p>${state.timelineOrdered ? `<form class="monogram-form" id="monogramForm"><div class="extraction-board"><span>第 2 张</span><i>+</i><span>第 5 张</span><i>+</i><span>第 7 张</span><i>=</i><input id="monogramInput" type="text" maxlength="3" autocomplete="off" placeholder="___" aria-label="三个字母的名字缩写" /></div><div class="puzzle-actions"><button class="button button--ghost" type="button" id="shuffleAgain">重新排列</button><button class="button button--primary" type="submit">确认名字</button></div></form>` : `<div class="puzzle-actions"><button class="button button--ghost" type="button" id="resetTimeline">恢复初始乱序</button><button class="button button--primary" type="button" id="checkTimeline">检查顺序</button></div>`}`, onOpen: () => { document.querySelectorAll('[data-move]').forEach((button) => button.addEventListener('click', () => { const index = Number(button.dataset.index); const direction = Number(button.dataset.move); state.timelineOrder = moveTimelineCard(state.timelineOrder, index, direction); state.timelineOrdered = isTimelineSolved(state.timelineOrder); saveState(); playClick(); openTimelinePuzzle(); })); document.querySelector('#resetTimeline')?.addEventListener('click', () => { state.timelineOrder = [...PUZZLE.timelineInitialOrder]; state.timelineOrdered = false; saveState(); openTimelinePuzzle(); }); document.querySelector('#checkTimeline')?.addEventListener('click', () => { const feedback = document.querySelector('#timelineFeedback'); if (!isTimelineSolved(state.timelineOrder)) { feedback.textContent = '还有学段没有回到正确位置。先找最早和最晚的两张，再整理中间。'; feedback.className = 'inline-feedback is-error'; playError(); return; } state.timelineOrdered = true; addJournal('timeline-order', '七张卡按初三、高一、高二、高三、大一、大二、大三排回了正确顺序。'); saveState(); playSuccess(); openTimelinePuzzle(); }); document.querySelector('#shuffleAgain')?.addEventListener('click', () => { state.timelineOrdered = false; state.timelineOrder = [...PUZZLE.timelineInitialOrder]; saveState(); openTimelinePuzzle(); }); const form = document.querySelector('#monogramForm'); if (form) { const input = document.querySelector('#monogramInput'); const feedback = document.querySelector('#timelineFeedback'); input.addEventListener('input', () => { input.value = normalizeLetters(input.value, 3); }); form.addEventListener('submit', (event) => { event.preventDefault(); const extracted = extractTimelineMonogram(state.timelineOrder); if (!isValidMonogram(input.value) || extracted !== PUZZLE.monogram) { feedback.textContent = '字母没有吻合。确认卡片顺序，并按猫项圈上的 2、5、7 从左向右读取。'; feedback.className = 'inline-feedback is-error'; playError(); return; } state.monogramSolved = true; addJournal('monogram', `第 2、5、7 张卡依次给出 ${PUZZLE.monogram}，名字重新长回树枝。`); saveState(); render(); playSuccess(); openSolvedTimeline(true); }); } } }); }
function openSolvedTimeline(justSolved = false) { openModal({ eyebrow: justSolved ? 'MEMORY 02 / RESTORED' : 'CLUE / NAME', title: '从初三走到大三的名字', wide: true, content: `<div class="solved-timeline">${PUZZLE.timelineOrder.map((id, index) => { const card = CARD_BY_ID.get(id); const picked = PUZZLE.extractPositions.includes(index + 1); return `<span class="${picked ? 'is-picked' : ''}"><small>${escapeHTML(card.stage)}</small><strong>${escapeHTML(card.letter)}</strong></span>`; }).join('')}</div><div class="reveal-word"><small>2 · 5 · 7</small><strong>${escapeHTML(PUZZLE.monogram)}</strong><p>猫给出位置，狗给出方向，时间给出名字。</p></div><div class="puzzle-actions"><button class="button button--primary" type="button" id="goFlowers">去看树下的花</button></div>`, onOpen: () => document.querySelector('#goFlowers').addEventListener('click', () => { closeModal(); highlightObject('flowers'); }) }); }
